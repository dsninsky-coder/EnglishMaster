# 英语大师 v2.0 技术实现方案（确认版）

> 版本：v1.1.0 起开发 | 日期：2026-08-06 | 状态：**已确认，开发中**

---

## 一、总体架构：听说大师（旧） vs 听力大师（新）

### 模块划分

| 模块 | 入口 | 系统 | 步骤 | 状态 |
|------|------|------|------|------|
| **听说大师** | 原学生端入口 | 旧 7 步 | Step0~7（不变） | 保留运行，不再改动 |
| **听力大师** | 新学生端入口 | 新 4 步 | Step1~4（重新设计） | **本次开发重点** |

- 两模块**共享一套课程原材料**（课程/句子/音频/单词表）
- 课程方案仅服务于听力大师
- 听说大师的旧课程分配、旧 7 步逻辑完全不动

---

## 二、新 4 步学习系统（听力大师）

### Step 1：单词熟悉（无金币）
- 展示该短文的**全部单词**（含冠词、介词、助词等虚词），按文中首次出现顺序排列
- 每个单词显示：**英文 + 音标(IPA) + 中文释义**
- 点击单词 → 调用**有道词典 API** 发音（`https://dict.youdao.com/dictvoice?audio=${word}&type=0`，美式发音）
- 纯学习环节，无评分、无金币，点「进入下一步」完成

### Step 2：句子理解（无金币）
- 逐句展示：**英文原文 + 音频播放 + 中文翻译**，三者同时可见
- 可切换：逐句模式 / 全文模式
- 音频支持倍速控制（0.5/0.8/1/1.2/1.5）
- 无评分、无金币，仔细看、仔细听、仔细理解后点「进入下一步」

### Step 3：辅助听写（有金币）
- 逐句练习（乱序）：
  - **给音**：播放该句音频（可无限重播）
  - **给义**：显示该句中文翻译
  - **写形**：学生输入英文原文
  - 判分：本地英文词匹配优先 → AI 评分兜底
  - **答错不显示正确答案**，提示「❌ 不对，再试试」，可修改直至答对
  - 记录每次提交的尝试次数
- 金币：首次答对 +2（尝试1次）/ +1（尝试2次）/ 0（尝试3次+），重复答题 0
- 全部句子通过 → 步骤完成

### Step 4：纯听写 + 翻译（有金币，拆为两个独立题目）
- 逐句练习（乱序），每句拆为两个独立提交：

  **题目 A — 听写形（给音，写形）**：
  - 播放该句音频（不显示中文）
  - 学生输入英文原文
  - 答错不显示答案，修改直至答对
  - 单独提交、单独判分、单独发金币

  **题目 B — 翻译成义（写义）**：
  - 显示该句英文原文（或可听音频辅助）
  - 学生输入中文翻译
  - 判分：本地中文相似度 → AI 兜底
  - 答错不显示答案，修改直至答对
  - 单独提交、单独判分、单独发金币

- **金币**：题目 A 和 B 各独立计算，每句最多 2×2=4 金币
- **提交**：两个独立的提交按钮，"提交听写" + "提交翻译"
- 两句都通过 → 该句完成

### 跳过题目机制（替代放弃，DH1）
- **「跳过本题」按钮**：学生在当前题答不出来时，可以点跳过
- **效果**：本题移到本轮队列末尾，等其他题做完了再回来答
- **必须全部答对**：所有题目（含被跳过的）都答对才算步骤完成
- **最终出口**：实在答不完 → 人工复议（2 金币）强制结束
- **无放弃机制**：不允许跳过不答而结束步骤

---

## 三、课程方案机制（管理员手动自由配置）

### 核心原则
- **管理员手动为每篇课程勾选步骤**，不依赖系统自动策略
- **极高自由度**：管理员可任意排列课程、任意勾选步骤组合
- 例如：课程1→Step1/2/3，课程2→Step1/2/3/4，课程3→Step1/3，课程4→Step3/4……
- 甚至允许某课程只有 Step 3 或只有 Step 4（跳过 Step 1/2）

### 数据模型（新增表）

```
CourseScheme（课程方案）
├── id, name(A/B/C...), description
├── max_errors_before_fallback（回退错误阈值，默认10）
├── cooldown_minutes（冷却时长，默认5）
├── is_active, created_at
│
├── CourseSchemeItem（方案文章配置）
│   ├── scheme_id, course_id, order_index（从1开始）
│   └── steps(JSON): 如 [1,2,3] 或 [1,2,3,4] 或 [3,4] 或 [1,3,4]
│
├── CourseSchemeStudent（方案分配学生）
│   └── scheme_id, student_id
│
└── SchemeAssignment（学生方案进度）
    ├── scheme_id, student_id, course_id
    ├── current_step, step_unlocks(JSON), completed_steps(JSON)
    ├── is_completed
    ├── step_error_counts(JSON): {"3":5,"4":2}
    ├── step_entered_at(JSON): 进入各步的时间戳
    ├── step_fallen_back(JSON): 是否曾从各步回退过
    ├── appeal_locked, appeal_suppressed(JSON)
    └── assigned_at
```

### SchemeStepProgress（逐题进度追踪，新增表）
```
├── scheme_id, student_id, course_id, sentence_id
├── question_type: "step3" | "step4_dictation" | "step4_translation"
├── attempt_count（本题总尝试次数）
├── ever_correct（是否曾答对）
├── first_correct_attempt（首次答对是第几次）
├── coins_awarded（已发金币）
├── skipped（是否被跳过等待回头）
├── last_attempt_at
```

### 管理员操作流程
1. **素材管理**（现有 tab，保持不变）：传音频、词色对齐、单词管理
2. **课程方案**（新增 tab）：
   - 新建方案 → 命名、设回退阈值/冷却时长
   - 添加课程 → 从课程池中选课、设排序
   - **为每篇课程手动勾选步骤**（多选：☑Step1 ☑Step2 ☑Step3 ☑Step4）
   - 可拖拽调整课程排序
   - 分配学生 → 选学生加入方案
   - 激活 → 学生端即可看到

### 学生端（听力大师）
- 进入听力大师 → 显示所属方案的课程列表
- 课程按 order_index 顺序排列
- 顺序解锁：第1篇完成 → 第2篇解锁
- 课程卡片显示当前步骤和进度
- 已完成的课程可回顾

---

## 四、单词管理重构

### CourseWord 表结构变更

| 字段 | 现状 | 变更 |
|------|------|------|
| word | ✅ 有 | 不变 |
| is_custom | ✅ 有 | 不变 |
| **meaning** | ❌ 无 | **新增**：中文释义，可编辑 |
| **phonetic** | ❌ 无 | **新增**：音标(IPA)，可编辑 |

### 全文单词提取（新逻辑）
- 旧逻辑：`extract_course_words()` 过滤虚词，仅保留实词
- 新逻辑：保留**全部单词**（含冠词/介词/助词等），去重，小写
- 迁移：对现有课程运行新提取，补充缺失单词（保留已有个性化修改）

### 音标获取方案
1. **首选**：`eng-to-ipa` Python 库（离线，基于 CMU 词典，美式音标）
2. **兜底**：AI 生成（DeepSeek，传单词+上下文）
3. 存储：`CourseWord.phonetic`，格式如 `/həˈloʊ/`
4. 两者均失败：phonetic=null，显示「—」

### 中文释义获取方案
1. **首选**：AI 生成（传入文章全文作为上下文，确保释义与文章语境一致）
2. 管理员可在素材管理界面逐个修改
3. 存储：`CourseWord.meaning`

### 单词发音（有道 API）
- 前端调用 `https://dict.youdao.com/dictvoice?audio=${encodeURIComponent(word)}&type=0`
- type=0 美式发音，type=1 英式发音
- 项目中已有 `playYoudao()` 函数，直接复用
- 零服务端成本，跨域可直接播放

### 管理员素材管理界面增强
- 单词列表新增列：音标、中文释义（均可内联编辑）
- 「提取全文单词」按钮（含虚词）
- 「批量生成音标」按钮
- 「批量生成释义」按钮
- 支持管理员手动修改任意单词的释义和音标

---

## 五、金币系统（听力大师）

### 规则

| 步骤 | 金币 | 说明 |
|------|------|------|
| Step 1 | 0 | 学习阶段，无金币 |
| Step 2 | 0 | 学习阶段，无金币 |
| Step 3（听写） | 每题独立 | 1次答对+2，2次+1，3次+ 0 |
| Step 4A（听写形） | 每题独立 | 同上 |
| Step 4B（翻译义） | 每题独立 | 同上 |
| 每日签到 | +1 | 现有机制不变 |

- Step 4 每句 **2 个独立题目 × 最多 2 金币 = 最多 4 金币/句**
- **重复答题**：`ever_correct=true` 后再答对 = 0 金币
- 提交即尝试，每次错误提交尝试次数+1

---

## 六、回退与冷却机制

### 回退
- **触发**：某步错误次数 ≥ `max_errors_before_fallback`（方案配置，默认 10）
- **操作**：学生点「回退到上一步」
- **效果**：当前步重新锁定，回退到上一步重新学习
- 重新进入时：已完成句子保留进度，错误计数清零

### 冷却
- 从某步回退过后，重新进入该步时启动冷却计时
- 冷却期间（`cooldown_minutes`，默认 5 分钟）不允许再次回退
- 防止「回退看答案→回来→再回退」刷答案循环

---

## 七、人工复议（保留）
- 复用现有 `Appeal` 表
- 2 金币/次
- 适用于：答不出的题目、被跳过后仍答不对的题目
- 管理员裁决：通过→退还金币+按尝试次数补发金币；驳回→没收金币

---

## 八、设计漏洞修复清单（最终确认版）

| 编号 | 问题 | 修复方案 | 状态 |
|------|------|---------|------|
| **DH1** | 答错不显答案导致卡死 | 跳过本题→放到队尾回头答；人工复议强制结束。**不允许放弃** | ✅ 已确认 |
| **DH2** | Step 4 金币计算 | 听写和翻译拆为**两个独立题目**，分开提交、分开计币 | ✅ 已确认 |
| **DH3** | 全文单词与现有单词合并 | 新提取保存全部词+虚词，按 word 去重合并，保留 is_custom | — |
| **DH4** | 音标获取可靠性 | eng-to-ipa 离线优先 + AI 兜底，失败显示「—」 | ✅ 已确认 |
| **DH5** | 释义与文章语境不符 | AI 生成时传入全文上下文，管理员可逐个修改 | — |
| **DH6** | 新旧系统并存 | **听力大师**（新4步）vs **听说大师**（旧7步），共享原材料 | ✅ 已确认 |
| **DH7** | Step 3/4 题目顺序 | 乱序（防死记），每次进入重新打乱 | — |
| **DH8** | 音频播放次数 | 无限重播，不影响金币 | — |
| **DH9** | 课程解锁逻辑 | 顺序解锁，首篇无锁，完成后解锁下篇 | — |
| **DH10** | 重复答题判定 | ever_correct=true 后不再发币 | — |
| **DH11** | 单词发音方案 | **有道词典 API**（项目已集成，`playYoudao()`） | ✅ 已确认 |
| **DH12** | 课程策略生成 | **管理员手动自由配置**，为每篇课程单独勾选步骤 | ✅ 已确认 |

---

## 九、数据库迁移策略

### 原则
- 绝不删除现有表和列
- 仅新增表和列（ALTER TABLE ADD COLUMN）
- 迁移前自动备份
- 迁移幂等（可重复执行）

### 迁移内容

| 类型 | 对象 | 操作 |
|------|------|------|
| 新增表 | course_schemes | CREATE TABLE |
| 新增表 | course_scheme_items | CREATE TABLE |
| 新增表 | course_scheme_students | CREATE TABLE |
| 新增表 | scheme_assignments | CREATE TABLE |
| 新增表 | scheme_step_progress | CREATE TABLE |
| 修改表 | course_words | ADD COLUMN meaning TEXT |
| 修改表 | course_words | ADD COLUMN phonetic TEXT |
| 修改表 | appeals | ADD COLUMN scheme_id INTEGER |
| 数据迁移 | course_words | 对现有词批量生成音标 + 释义 |
| 数据迁移 | course_words | 对现有课程补充全文单词 |

---

## 十、API 设计概要

### 课程方案管理
- `POST/GET/PUT/DELETE /api/admin/scheme` — 方案 CRUD
- `POST /api/admin/scheme/<id>/items` — 添加课程到方案
- `PUT /api/admin/scheme/<id>/item/<iid>` — 修改课程步骤配置
- `DELETE /api/admin/scheme/<id>/item/<iid>` — 从方案移除课程
- `POST /api/admin/scheme/<id>/students` — 分配学生
- `DELETE /api/admin/scheme/<id>/student/<sid>` — 移除学生
- `POST /api/admin/scheme/<id>/activate` — 激活推送

### 单词管理增强
- `POST /api/admin/course/<id>/extract-all-words` — 提取全文单词
- `POST /api/admin/course/<id>/generate-phonetics` — 批量生成音标
- `POST /api/admin/course/<id>/generate-meanings` — 批量生成释义
- `PUT /api/admin/course/<id>/word/<wid>` — 修改单词（释义+音标）

### 听力大师学生端
- `GET /api/scheme/courses` — 方案课程列表
- `GET /api/scheme/course/<id>/learn` — 进入学习
- `POST /api/scheme/step/submit` — 提交答案
- `POST /api/scheme/step/finish` — 完成步骤
- `POST /api/scheme/step/backstep` — 回退
- `POST /api/scheme/step/skip-question` — 跳过本题（放队尾）
- `POST /api/scheme/step/appeal` — 人工复议

---

## 十一、实施阶段

| 阶段 | 版本 | 内容 |
|------|------|------|
| A | v1.1.0 | 发布 v1.0 + 数据库迁移 + 单词管理重构（全文词+音标+释义） |
| B | v1.2.0 | 新 4 步学习系统核心（Step1~4 渲染+判分+金币+跳过+有道发音） |
| C | v1.3.0 | 课程方案机制（方案 CRUD + 手动步骤配置 + 学生分配） |
| D | v1.4.0 | 回退+冷却+复议 + 听力大师学生端完整流程 |
| E | v1.5.0 | 两端 UI 打磨 + 端到端测试 + 边界处理 |

---

## 十二、风险与注意事项

1. **数据库安全**：迁移前自动备份，迁移幂等
2. **新旧兼容**：听说大师 7 步完全不动，听力大师独立运行
3. **离线运行**：无 AI Key 时判分降级为本地匹配，音标用 eng-to-ipa 离线生成
4. **有道 API 依赖**：单词发音需联网，但句子音频用本地 mp3（不受影响）
5. **测试铁律**：测试脚本不放 backend/ 内，用 127.0.0.1
